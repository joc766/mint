# Example override file for local development customization
# Copy this to docker-compose.override.yml (git ignored) to customize your local setup
# Usage: docker-compose up (automatically uses override file if present)

services:
  frontend:
    environment:
      # Override API URL for local development
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    # Add any other local overrides here
